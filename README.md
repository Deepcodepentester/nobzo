<h1> saw the email very late</h1>
RUn npm install
RUn node index.js


<p>Since it is a small project no reafresh token was used and the tokens are not short lived but token is authenticated in the http only cookie</p>
